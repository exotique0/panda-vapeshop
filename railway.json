{
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "python3 -m ensurepip && python3 -m pip install -r bot/requirements.txt"
  },
  "deploy": {
    "startCommand": "cd bot && python3 main.py"
  }
}
